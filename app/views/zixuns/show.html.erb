<% provide(:title, @zixun.title) %>
<%= render 'home/nav_list' %>
<div class="product-index">
	<div class="about-title">
		<h4><%= @zixun.title %></h4>
		<div class="breadcrumb">
			<%= render_navigation(renderer: :breadcrumbs, join_with: '&nbsp / &nbsp') %>
		</div>
	</div>
		<div class="about-content">
			<%= raw @zixun.content %>
		</div>
	</div>
