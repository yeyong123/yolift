<%= render 'home/nav_list' %>
<div class="product-index">
	<table class="table">
		<% @cases.each do |ca| %>
			<tr>
				<td><%= image_tag(ca.image_url(:small)) %></td>
				<td><%= link_to ca.title, case_path(ca.id) %></td>
			</tr>
		<% end %>
	</table>
	<%= will_paginate @cases %>
</div>
