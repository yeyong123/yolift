<%= render 'home/nav_list' %>
<div class="product-index">
	<%= form_for @contact_image do |f| %>
		<%= render 'home/error_message', object: f.object %>
		<%= render partial: 'product_images/new', locals: {f: f, object: @contact_image} %>
	<% end %>
</div>
