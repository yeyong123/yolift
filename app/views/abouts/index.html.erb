<% provide(:title, "关于我们") %>
<%= render 'home/nav_list' %>
<div class="product-index">
	<table class="table">
		<% @abouts.each do |about| %>
			<tr>
				<td><%= link_to about.title, about_path(about) %></td>
							</tr>
							<% end %>
		</table>
		<%= will_paginate @abouts %>
			</div>
