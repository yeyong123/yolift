<div class="product-index">
	<%= form_for @server_image do |f| %>
		<%= render 'home/error_message', object: f.object %>
		<%= render partial: 'product_images/new', locals: {f: f, object: @server_image} %>
	<% end %>
</div>
