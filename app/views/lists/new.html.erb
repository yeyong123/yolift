<%= render 'home/nav_list' %>
<div class="product-index">
	<%= form_for @list do |f| %>
		<div class="field">
			<%= f.label :image, "导航展示图" %>
			<%= image_tag(@list.image_url(:small)) if @list.image_url %>
			<%= f.file_field :image %>
		</div>
	<%= render partial: 'lists/form', locals: {f: f} %> 
	<div class="about-action">
		<%= f.submit "保存",class: "btn btn-success" %>
		<%= link_to "返回", :back %>
	</div>
<% end %>
</div>
