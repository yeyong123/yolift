<%= render 'home/nav_list' %>
<div class="product-index">
	<h5>采购指南</h5>
	<table class="table">
		<% @lists.each do |list| %>
		<tr>
			<td><%= link_to list.title,list_path(list) %></td>
		</tr>
	<% end %>
	</table>
</div>
