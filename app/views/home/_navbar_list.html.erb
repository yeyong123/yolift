<div class="list-nav">
	<div class="navbar">
		<div class="navbar-inner">
			<a class="btn btn-navbar" data-toggle="collapse" data-target=".nav-collapse">
				<span class="icon-bar"></span>
				<span class="icon-bar"></span>
				<span class="icon-bar"></span>
			</a>
			<div class="nav-collapse collapse">
				<ul class="nav">
					<li class="product-menu"><span>产品目录<span>&gt&gt</span></span>
					<% if request.path != root_path %>
					<div id="myaffix">
						<%= render 'home/nav_list' %>
					</div>
				<% end %>
					</li>
					<li class="active index"><%= link_to "首页", root_path %></li>
					<li class="dropdown"><%= link_to "关于我们", about_path(About.first),class: "dropdown-toggle" %>
					<ul class="dropdown-menu">
						<% About.limit(5).each do |about| %>
							<li><%= link_to about.title, about_path(about.id)%></li>
						<% end %>
					</ul>
					</li>
					<li class="dropdown"><%= link_to "产品中心", products_path, class: "dropdown-toggle" %>
					<ul class="dropdown-menu">
						<% Category.limit(7).each do |cate| %>
						<li><%= link_to cate.name, category_path(cate.id) %></li>
					<% end %>
					</ul>
					</li>
						<li class="dropdown"><%= link_to "新闻资讯", server_path(Server.first), class: "dropdown-toggle" %>
					<ul class="dropdown-menu">
						<% Server.limit(3).each do |server| %>
							<li><%= link_to server.title, server_path(server.id)  %></li>
						<% end %>
					</ul>
					</li>
					<li class="dropdown"><%= link_to "服务中心", center_path(Center.first), class: "dropdown-toggle" %>
					<ul class="dropdown-menu">
					<% Center.limit(4).each do |center| %>
						<% unless center.title == "售后服务" %>
						<li><%= link_to center.title, center_path(center.id) %></li>
					<% end %>
					<% end %>
						<li><%= link_to "售后服务", aftermarkets_path %></li>
					</ul>
					</li>
					<li class="dropdown"><%= link_to t('.contact'), contact_path(Contact.first), class: "dropdown-toggle" %>
					<ul class="dropdown-menu">
						<% Contact.limit(4).each do |contact| %>
							<li><%= link_to contact.title, contact_path(contact)  %></li>
						<% end %>
					</ul>
					</li>
					<li class="chuxiao"><%= link_to Contact.last.title, contact_path(Contact.last) %></li>
				</ul>
				<form action="/search" method="get" class="navbar-form pull-right">
					<input type="text" name="q" value="" class="serach-query" placeholder="输入产品名称或者货号">
					<button type="submit" id="search-get" class="btn">搜索</button>
					<span class="cart-img"><%= image_tag("logo/cart.png",alt: "购物车")%></span>
					<span class="number-cart"><img src="/assets/number.png"/><b><%= @cart.total_items %></b>
					</span>
					<%= render 'carts/cart_item' %>
				</form>
			</div><!--/.nav-collapse -->
		</div>
	</div>
</div>

