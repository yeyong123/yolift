<%= render 'home/nav_list' %>
<div class="product-index">
<%= form_for @job do |f| %>
	<%= render partial: 'jobs/form', locals: {f: f} %>
	<div class="about-action">
		<%= f.submit "发布职位", class: "btn btn-success" %>
		<%= link_to "返回", jobs_path %>
	</div>
<% end %>
</div>
