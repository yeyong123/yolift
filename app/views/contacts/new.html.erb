<%= form_for @contact do |f| %>
	<%= render partial: 'abouts/form', locals: {f: f} %>
	<div class="about-action">
		<%= f.submit "保存", class: "btn btn-success" %>
		<%= link_to "返回", contacts_path %>
	<% end %>	
