<%= render 'home/nav_list' %>
<div class="product-index">
	<table class="table">
		<% @payments.each do |payment| %>
			<%= render partial: "payments/index", locals: {payment: payment} %>
		<% end %>
	</table>
	<%= will_paginate @payments %>
</div>
