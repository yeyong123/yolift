<div class="product-index">
	<%= form_for [:admin, @about_image] do |f| %>
		<%= render 'home/error_message', object: f.object %>
		<%= render partial: 'product_images/new', locals: {f: f, object: @about_image} %>
	<% end %>
</div>
